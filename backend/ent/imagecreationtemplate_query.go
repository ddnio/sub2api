// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/imagecreationasset"
	"github.com/Wei-Shaw/sub2api/ent/imagecreationtemplate"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/user"
)

// ImageCreationTemplateQuery is the builder for querying ImageCreationTemplate entities.
type ImageCreationTemplateQuery struct {
	config
	ctx                *QueryContext
	order              []imagecreationtemplate.OrderOption
	inters             []Interceptor
	predicates         []predicate.ImageCreationTemplate
	withDraftCover     *ImageCreationAssetQuery
	withPublishedCover *ImageCreationAssetQuery
	withCreator        *UserQuery
	withUpdater        *UserQuery
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ImageCreationTemplateQuery builder.
func (_q *ImageCreationTemplateQuery) Where(ps ...predicate.ImageCreationTemplate) *ImageCreationTemplateQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ImageCreationTemplateQuery) Limit(limit int) *ImageCreationTemplateQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ImageCreationTemplateQuery) Offset(offset int) *ImageCreationTemplateQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ImageCreationTemplateQuery) Unique(unique bool) *ImageCreationTemplateQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ImageCreationTemplateQuery) Order(o ...imagecreationtemplate.OrderOption) *ImageCreationTemplateQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryDraftCover chains the current query on the "draft_cover" edge.
func (_q *ImageCreationTemplateQuery) QueryDraftCover() *ImageCreationAssetQuery {
	query := (&ImageCreationAssetClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(imagecreationtemplate.Table, imagecreationtemplate.FieldID, selector),
			sqlgraph.To(imagecreationasset.Table, imagecreationasset.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, imagecreationtemplate.DraftCoverTable, imagecreationtemplate.DraftCoverColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPublishedCover chains the current query on the "published_cover" edge.
func (_q *ImageCreationTemplateQuery) QueryPublishedCover() *ImageCreationAssetQuery {
	query := (&ImageCreationAssetClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(imagecreationtemplate.Table, imagecreationtemplate.FieldID, selector),
			sqlgraph.To(imagecreationasset.Table, imagecreationasset.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, imagecreationtemplate.PublishedCoverTable, imagecreationtemplate.PublishedCoverColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreator chains the current query on the "creator" edge.
func (_q *ImageCreationTemplateQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(imagecreationtemplate.Table, imagecreationtemplate.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, imagecreationtemplate.CreatorTable, imagecreationtemplate.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUpdater chains the current query on the "updater" edge.
func (_q *ImageCreationTemplateQuery) QueryUpdater() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(imagecreationtemplate.Table, imagecreationtemplate.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, imagecreationtemplate.UpdaterTable, imagecreationtemplate.UpdaterColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ImageCreationTemplate entity from the query.
// Returns a *NotFoundError when no ImageCreationTemplate was found.
func (_q *ImageCreationTemplateQuery) First(ctx context.Context) (*ImageCreationTemplate, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{imagecreationtemplate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) FirstX(ctx context.Context) *ImageCreationTemplate {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ImageCreationTemplate ID from the query.
// Returns a *NotFoundError when no ImageCreationTemplate ID was found.
func (_q *ImageCreationTemplateQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{imagecreationtemplate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ImageCreationTemplate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ImageCreationTemplate entity is found.
// Returns a *NotFoundError when no ImageCreationTemplate entities are found.
func (_q *ImageCreationTemplateQuery) Only(ctx context.Context) (*ImageCreationTemplate, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{imagecreationtemplate.Label}
	default:
		return nil, &NotSingularError{imagecreationtemplate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) OnlyX(ctx context.Context) *ImageCreationTemplate {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ImageCreationTemplate ID in the query.
// Returns a *NotSingularError when more than one ImageCreationTemplate ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ImageCreationTemplateQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{imagecreationtemplate.Label}
	default:
		err = &NotSingularError{imagecreationtemplate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ImageCreationTemplates.
func (_q *ImageCreationTemplateQuery) All(ctx context.Context) ([]*ImageCreationTemplate, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ImageCreationTemplate, *ImageCreationTemplateQuery]()
	return withInterceptors[[]*ImageCreationTemplate](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) AllX(ctx context.Context) []*ImageCreationTemplate {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ImageCreationTemplate IDs.
func (_q *ImageCreationTemplateQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(imagecreationtemplate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ImageCreationTemplateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ImageCreationTemplateQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ImageCreationTemplateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ImageCreationTemplateQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ImageCreationTemplateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ImageCreationTemplateQuery) Clone() *ImageCreationTemplateQuery {
	if _q == nil {
		return nil
	}
	return &ImageCreationTemplateQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]imagecreationtemplate.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.ImageCreationTemplate{}, _q.predicates...),
		withDraftCover:     _q.withDraftCover.Clone(),
		withPublishedCover: _q.withPublishedCover.Clone(),
		withCreator:        _q.withCreator.Clone(),
		withUpdater:        _q.withUpdater.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithDraftCover tells the query-builder to eager-load the nodes that are connected to
// the "draft_cover" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ImageCreationTemplateQuery) WithDraftCover(opts ...func(*ImageCreationAssetQuery)) *ImageCreationTemplateQuery {
	query := (&ImageCreationAssetClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDraftCover = query
	return _q
}

// WithPublishedCover tells the query-builder to eager-load the nodes that are connected to
// the "published_cover" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ImageCreationTemplateQuery) WithPublishedCover(opts ...func(*ImageCreationAssetQuery)) *ImageCreationTemplateQuery {
	query := (&ImageCreationAssetClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPublishedCover = query
	return _q
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ImageCreationTemplateQuery) WithCreator(opts ...func(*UserQuery)) *ImageCreationTemplateQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreator = query
	return _q
}

// WithUpdater tells the query-builder to eager-load the nodes that are connected to
// the "updater" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ImageCreationTemplateQuery) WithUpdater(opts ...func(*UserQuery)) *ImageCreationTemplateQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUpdater = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		State string `json:"state,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ImageCreationTemplate.Query().
//		GroupBy(imagecreationtemplate.FieldState).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ImageCreationTemplateQuery) GroupBy(field string, fields ...string) *ImageCreationTemplateGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ImageCreationTemplateGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = imagecreationtemplate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		State string `json:"state,omitempty"`
//	}
//
//	client.ImageCreationTemplate.Query().
//		Select(imagecreationtemplate.FieldState).
//		Scan(ctx, &v)
func (_q *ImageCreationTemplateQuery) Select(fields ...string) *ImageCreationTemplateSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ImageCreationTemplateSelect{ImageCreationTemplateQuery: _q}
	sbuild.label = imagecreationtemplate.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ImageCreationTemplateSelect configured with the given aggregations.
func (_q *ImageCreationTemplateQuery) Aggregate(fns ...AggregateFunc) *ImageCreationTemplateSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ImageCreationTemplateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !imagecreationtemplate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ImageCreationTemplateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ImageCreationTemplate, error) {
	var (
		nodes       = []*ImageCreationTemplate{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withDraftCover != nil,
			_q.withPublishedCover != nil,
			_q.withCreator != nil,
			_q.withUpdater != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ImageCreationTemplate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ImageCreationTemplate{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withDraftCover; query != nil {
		if err := _q.loadDraftCover(ctx, query, nodes, nil,
			func(n *ImageCreationTemplate, e *ImageCreationAsset) { n.Edges.DraftCover = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPublishedCover; query != nil {
		if err := _q.loadPublishedCover(ctx, query, nodes, nil,
			func(n *ImageCreationTemplate, e *ImageCreationAsset) { n.Edges.PublishedCover = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCreator; query != nil {
		if err := _q.loadCreator(ctx, query, nodes, nil,
			func(n *ImageCreationTemplate, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUpdater; query != nil {
		if err := _q.loadUpdater(ctx, query, nodes, nil,
			func(n *ImageCreationTemplate, e *User) { n.Edges.Updater = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ImageCreationTemplateQuery) loadDraftCover(ctx context.Context, query *ImageCreationAssetQuery, nodes []*ImageCreationTemplate, init func(*ImageCreationTemplate), assign func(*ImageCreationTemplate, *ImageCreationAsset)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ImageCreationTemplate)
	for i := range nodes {
		if nodes[i].DraftCoverAssetID == nil {
			continue
		}
		fk := *nodes[i].DraftCoverAssetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(imagecreationasset.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "draft_cover_asset_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ImageCreationTemplateQuery) loadPublishedCover(ctx context.Context, query *ImageCreationAssetQuery, nodes []*ImageCreationTemplate, init func(*ImageCreationTemplate), assign func(*ImageCreationTemplate, *ImageCreationAsset)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ImageCreationTemplate)
	for i := range nodes {
		if nodes[i].PublishedCoverAssetID == nil {
			continue
		}
		fk := *nodes[i].PublishedCoverAssetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(imagecreationasset.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "published_cover_asset_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ImageCreationTemplateQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*ImageCreationTemplate, init func(*ImageCreationTemplate), assign func(*ImageCreationTemplate, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ImageCreationTemplate)
	for i := range nodes {
		fk := nodes[i].CreatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ImageCreationTemplateQuery) loadUpdater(ctx context.Context, query *UserQuery, nodes []*ImageCreationTemplate, init func(*ImageCreationTemplate), assign func(*ImageCreationTemplate, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ImageCreationTemplate)
	for i := range nodes {
		fk := nodes[i].UpdatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "updated_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ImageCreationTemplateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ImageCreationTemplateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(imagecreationtemplate.Table, imagecreationtemplate.Columns, sqlgraph.NewFieldSpec(imagecreationtemplate.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, imagecreationtemplate.FieldID)
		for i := range fields {
			if fields[i] != imagecreationtemplate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withDraftCover != nil {
			_spec.Node.AddColumnOnce(imagecreationtemplate.FieldDraftCoverAssetID)
		}
		if _q.withPublishedCover != nil {
			_spec.Node.AddColumnOnce(imagecreationtemplate.FieldPublishedCoverAssetID)
		}
		if _q.withCreator != nil {
			_spec.Node.AddColumnOnce(imagecreationtemplate.FieldCreatedBy)
		}
		if _q.withUpdater != nil {
			_spec.Node.AddColumnOnce(imagecreationtemplate.FieldUpdatedBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ImageCreationTemplateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(imagecreationtemplate.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = imagecreationtemplate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *ImageCreationTemplateQuery) ForUpdate(opts ...sql.LockOption) *ImageCreationTemplateQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *ImageCreationTemplateQuery) ForShare(opts ...sql.LockOption) *ImageCreationTemplateQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// ImageCreationTemplateGroupBy is the group-by builder for ImageCreationTemplate entities.
type ImageCreationTemplateGroupBy struct {
	selector
	build *ImageCreationTemplateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ImageCreationTemplateGroupBy) Aggregate(fns ...AggregateFunc) *ImageCreationTemplateGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ImageCreationTemplateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ImageCreationTemplateQuery, *ImageCreationTemplateGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ImageCreationTemplateGroupBy) sqlScan(ctx context.Context, root *ImageCreationTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ImageCreationTemplateSelect is the builder for selecting fields of ImageCreationTemplate entities.
type ImageCreationTemplateSelect struct {
	*ImageCreationTemplateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ImageCreationTemplateSelect) Aggregate(fns ...AggregateFunc) *ImageCreationTemplateSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ImageCreationTemplateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ImageCreationTemplateQuery, *ImageCreationTemplateSelect](ctx, _s.ImageCreationTemplateQuery, _s, _s.inters, v)
}

func (_s *ImageCreationTemplateSelect) sqlScan(ctx context.Context, root *ImageCreationTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
